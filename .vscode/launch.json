{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Start App",
            "request": "launch",
            "command": "npm run start",
            "cwd": "${workspaceFolder}"
        }
    ]
}